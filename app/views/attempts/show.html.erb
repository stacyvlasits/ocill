<%- model_class = Attempt -%>
<div class="page-header">
  <h1><%=t '.title', :default => t('helpers.titles.new', :model => model_class.model_name.human, :default => "Graded #{model_class.model_name.human}") %></h1>
</div>
<%= render :partial => nested_folder(@attempt) + 'attempt_drill', :locals => {:attempt => @attempt, :drill => @attempt.drill} %>
