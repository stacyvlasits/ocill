<li>
	<%= role.user.email %>
  	<% if can? :manage, @course %> 
	    <%= link_to icon("remove"), role_path(role), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) } %>
	<% end -%>

</li>